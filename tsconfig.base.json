{
  "$schema": "https://json.schemastore.org/tsconfig",
  "compilerOptions": {
    "target": "ES2022",
    "lib": ["ES2022"],
    "module": "ESNext",
    "moduleResolution": "Bundler",
    "esModuleInterop": true,
    "isolatedModules": true,
    "resolveJsonModule": true,
    "allowSyntheticDefaultImports": true,
    "forceConsistentCasingInFileNames": true,

    // ── strict family (CLAUDE.md §5「TypeScript (strict mode)」を担保) ──
    "strict": true,
    "noImplicitAny": true,
    "strictNullChecks": true,
    "strictFunctionTypes": true,
    "strictBindCallApply": true,
    "strictPropertyInitialization": true,
    "noImplicitThis": true,
    "alwaysStrict": true,
    "noUncheckedIndexedAccess": true,
    "noImplicitOverride": true,

    // ── 一般的な品質ルール ──
    "noUnusedLocals": false,
    "noUnusedParameters": false,
    "noFallthroughCasesInSwitch": true,
    "noPropertyAccessFromIndexSignature": false,

    // ── 型チェック性能 ──
    "skipLibCheck": true,
    "incremental": true,

    // ── path alias: lib/ 共通型を `@workspace/lib/*` で参照 ──
    // (8.96: baseUrl は TS6 deprecated / TS7 廃止。TS5+ は paths を「宣言した
    //  tsconfig の位置」基準で解決するので、この base が repo root にある限り同義)
    "paths": {
      "@workspace/lib": ["./lib"],
      "@workspace/lib/*": ["./lib/*"]
    }
  },
  "exclude": ["**/node_modules", "**/dist", "**/build", "**/.expo", "**/.next"]
}
